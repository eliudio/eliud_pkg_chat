name: eliud_pkg_chat
description: A new Flutter package.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_lints: ^1.0.0

  eliud_core:
    path: ../eliud_core

  cupertino_icons: ^1.0.3
  faker: ^2.0.0
  file_picker: ^3.0.2+2
#  firebase_auth: ^1.4.1
#  firebase_core: ^1.3.0
#  firebase_storage: ^8.1.3
  flutter_chat_ui: ^1.1.1
  flutter_firebase_chat_core: 1.1.0
  http: ^0.13.3
  mime: ^1.0.0
  open_file: ^3.2.1
  path_provider: ^2.0.2

dev_dependencies:
  build_runner: "1.11.5"

  eliud_generator:
    path: ../eliud_generator

flutter:
  uses-material-design: true
